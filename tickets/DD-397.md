# DD-397: Add Migration-Scale Read And Correction Tools

## Method

ATDD + migration BDD + security TDD.

## Objective

Replace hundreds of per-record process launches with one-call reads, previewed
bulk corrections, field-aware search, and append-only log annotations.

## Acceptance criteria

- Record list accepts `--full` without changing its compatible array shape.
- Export returns every SOW, epic, and ticket with column state in one object.
- Search with `--field` reports the exact matching field and value.
- Import accepts a ref-keyed JSON object, previews exact field changes with
  `--dry-run`, and applies all records transactionally.
- Replace previews field-level regex changes and applies only mutable content.
- Gate and evidence entries have stable IDs and append correction annotations
  without rewriting original observations.
- No polling watcher, general gate/evidence mutation, undo, or changelog is
  introduced.
- Docker tests retain 100% statement/subroutine coverage, followed by the
  taint/perlsec, documentation, commit, and push gates.

## Checklist

- [x] Record the ticket and select ATDD + migration BDD + security TDD.
- [x] Add executable one-call read and field-hit tests.
- [x] Add dry-run and atomic bulk mutation tests.
- [x] Add append-only annotation tests.
- [x] Implement the minimum command ecosystem.
- [x] Pass Docker coverage and security gates.
- [x] Update README, SKILLS.md, docs, Changes, and project records.
- [x] Commit the completed ticket.
- [ ] Push the completed ticket; blocked by the unavailable SSH key agent.

## Evidence

- Docker: 14 files and 574 assertions passed.
- Coverage: `lib/Tira.pm` and `lib/Tira/CLI.pm` each reached 100.0% statement
  and subroutine coverage.
- One-call reads: export returned every type with column state; full list kept
  its compatible array shape; field search returned dotted hit locations.
- Bulk safety: dry runs changed nothing, mixed valid/invalid imports were
  rejected atomically, exact imports applied transactionally, and replacement
  was restricted to mutable content.
- Append-only logs: stable gate/evidence IDs and attributed annotations passed
  while original entries remained unchanged.
- CLI: export, import, replace, full list, field search, both annotation routes,
  and symmetric aliases passed through the shared dispatcher.
- Security: all 574 assertions passed under taint mode, all 80 entrypoints
  compiled under taint mode, and the production process-primitive scan was
  clean.
- `SKILLS.md` retains exactly 100 implemented use cases and preserves managed
  project-location secrecy.
- No platform-dependent branch changed; macOS and Windows remain stopped.

## Release gate

The implementation was committed as `c9006f9`. The mandatory push was
attempted and rejected because the configured passphrase-protected SSH identity
has no active agent socket.

Status: verified and committed; release blocked on push.
